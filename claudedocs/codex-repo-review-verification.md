# Codex repository-review verification

**Verified:** 2026-07-13
**Baseline:** `origin/main` at `fbb733fa87a8641ed5f2b12fb131e9134a89a241`
**Scope:** C1–C3 and H1–H15 from
`claudedocs/repo-review-findings-2026-07-12.md`

This report was written before any implementation edit on branch
`fix/repo-review-critical-high`. Verification was read-only and used current
source, existing tests, targeted searches, and disposable local reproductions.
No server was contacted and no production data was read or changed.

## Verdicts

| ID | Verdict | Current-code evidence | Planned action |
|---|---|---|---|
| C1 | **MODIFIED** | `api/app/db/run_migrations.py:103-104` returns every tracking row without filtering `status`; failures are persisted at `api/app/db/run_migrations.py:197-215`; the run loop skips every returned name at `api/app/db/run_migrations.py:353-360`. A disposable SQLite reproduction confirmed a failed row is returned as applied. The proposed status filter alone is insufficient: the later success insert at `api/app/db/run_migrations.py:179-187` then collides with the existing unique failed row. | Filter applied rows to `status='success'` **and** upsert the success record over a prior failed row, clearing failure metadata. Add a failed-row retry regression. |
| C2 | **CONFIRMED** | `.github/workflows/ci.yml:10-147` defines Python, security, and offline staff-alignment jobs only. `web/package.json:7-13` defines build, lint, Jest, and Playwright commands, but the workflow contains no Node setup or `npm` invocation. | Add a frontend job with `npm ci`, lint, `tsc --noEmit`, and Jest. The full Playwright suite is not hermetic (several specs require a running stack/restarts or live behavior), so do not pretend it is covered by a unit CI job; retain it for a separately configured E2E gate. |
| C3 | **MODIFIED** | `.github/workflows/ci.yml:64-67` appends `|| echo` to mypy, guaranteeing a successful step. Running that command without the fallback currently reports a small, concrete baseline: stale `type: ignore` comments plus two Qdrant point-ID type errors. | Remove the fallback and fix the current errors directly. Excluding whole files would hide future regressions and is unnecessary for this baseline. |
| H1 | **CONFIRMED** | `api/app/db/run_migrations.py:172-195` calls bare `executescript()` and then `rollback()`. A disposable two-statement migration confirmed the first `CREATE TABLE` survives when the second statement fails. The down path repeats the pattern at `api/app/db/run_migrations.py:279-294`. | Put `BEGIN IMMEDIATE` inside the executed script, leave the transaction open through the tracking-row write, and commit once. Roll back partial up/down DDL on error. Add a statement-two-fails atomicity test. |
| H2 | **CONFIRMED** | nginx accepts any 32-hex `session_id` cookie as the rate key at `docker/nginx/nginx.conf:37-54`. The web identity hashes the same client-supplied cookie at `api/app/channels/plugins/web/identity.py:20-35`, the chat route assigns that identity at `api/app/routes/chat.py:293-305`, and the app bucket keys on `user_id` at `api/app/channels/middleware/rate_limit.py:55-83`. Only connection limiting stays address-based (`docker/nginx/nginx.conf:57-60`). | Add an address-based request ceiling alongside the session zone so rotating a cookie cannot reset every limiter, and add an application-wide daily LLM budget circuit breaker. Keep the session limiter as the fair-use layer; document that Tor can collapse address identity and therefore needs conservative/global bounds. |
| H3 | **MODIFIED** | The cited line drifted. The blocking call is now `self.llm.invoke(...)` at `api/app/services/simplified_rag_service.py:691-732`, inside async `_answer_from_context`. It affects the conversation-history/no-document fallback, not every generation path. `AISuiteLLMWrapper.invoke` is synchronous at `api/app/services/rag/llm_provider.py:355-391` and has no explicit timeout argument. | Run the call in `asyncio.to_thread` under an explicit timeout, preserving the existing insufficient-information fallback on timeout/error. Add a test proving the event loop remains responsive and a timeout test. |
| H4 | **CONFIRMED** | A forced rebuild deletes the configured collection at `api/app/services/rag/qdrant_index_manager.py:223-239`; rebuild invokes it before embedding/upserting all batches at `api/app/services/rag/qdrant_index_manager.py:440-506`. No alias operation exists in app/tests, while the retriever reads the same configured collection directly (`api/app/services/rag/qdrant_hybrid_retriever.py:69,273,304`). | Introduce a stable active alias, build a uniquely named physical collection, verify its count/status, atomically switch the alias, then delete the old physical collection. Preserve the old collection on any pre-swap failure. Add mock-client blue/green success/failure tests. |
| H5 | **CONFIRMED** | `Bisq2Channel` resets its cursor and seen IDs on construction (`api/app/channels/plugins/bisq2/channel.py:510-515`). Startup primes REST only after successful WebSocket startup (`api/app/channels/plugins/bisq2/channel.py:215-220`); without that, the first poll sends `since=None` (`api/app/channels/plugins/bisq2/channel.py:419-445`). The API omits the `since` query when falsey (`api/app/channels/plugins/bisq2/client/api.py:589-604`). The shared coordination store is process-local (`api/app/channels/coordination.py:41-76`). An existing persistent `BisqSyncStateManager` is unused (`api/app/channels/plugins/bisq2/client/sync_state.py:16-142`). | TDD: wire the existing persistent cursor/processed-ID manager into the channel, prime regardless of WebSocket outcome, and never allow a fresh REST fallback to use `since=None`. Persist the high-water mark so downtime messages are not dropped. Test absent/failed WebSocket plus process restart, historic suppression, and one genuinely new message. |
| H6 | **MODIFIED** | Direct transport failure returns `False` at `api/app/channels/response_dispatcher.py:170-224`; successful escalation persistence/notification also ends in `False` at `api/app/channels/response_dispatcher.py:226-242`. The orchestrator bool-casts the result (`api/app/channels/inbound_orchestrator.py:110-138`), and the coordinator retries two false results, re-attempts escalation, and sends a failure notice (`api/app/channels/arbitration/coordinator.py:458-480`). Escalation creation itself is idempotent (`api/app/services/escalation/escalation_service.py:97-111`), so rows are not duplicated, but create attempts and notices are. | TDD a typed `SENT`/`QUEUED`/`FAILED` outcome through dispatcher, orchestrator callback, and coordinator. Treat persisted queueing as terminal; retry only real delivery failure. Verify one queue attempt/notice set and no false failure message. |
| H7 | **CONFIRMED** | Bisq registers `staff_resolver` with Bisq profile IDs at `api/app/channels/plugins/bisq2/channel.py:101-110`; Matrix overwrites the same key at `api/app/channels/plugins/matrix/channel.py:161-168`. Enabled plugins are set up in sorted ID order (`api/app/channels/bootstrapper.py:429-440`), and Bisq later resolves the overwritten shared key dynamically (`api/app/channels/plugins/bisq2/channel.py:1066-1073`). | TDD channel-namespaced resolver keys and update Matrix/Bisq message, reaction, and ChatOps consumers. Prove dual-channel bootstrap cannot clobber either identity set. Do not union unrelated channel identifiers into a cross-channel authorization namespace. |
| H8 | **MODIFIED** | Staff answer/status is saved before delivery at `api/app/services/escalation/escalation_service.py:230-248`, then non-web delivery is recorded `DELIVERED`/`FAILED` at `api/app/services/escalation/escalation_service.py:264-307`. Same-staff repeat returns early for `RESPONDED` at `api/app/services/escalation/escalation_service.py:206-211`; `ESCALATION_DELIVERY_MAX_RETRIES` exists but is unused (`api/app/core/config.py:502-509`). ChatOps reports success regardless of failed delivery (`api/app/channels/chatops/dispatcher.py:347-402`). The accepted ADR requires save-first semantics and separate bounded delivery state (`claudedocs/adr/escalation-lifecycle-decisions.md:15-27`), so moving answer persistence after transport would violate the design. | TDD: keep save-first answer/status, set non-web delivery `PENDING` before transport, record attempts and `FAILED`, return an honest ChatOps result, and add idempotent bounded redelivery from the persisted staff answer. Test failure, retry success, max attempts, and web `NOT_REQUIRED`. |
| H9 | **CONFIRMED** | `CompositeTrustAlertPublisher.publish` requires `get_running_loop()` and drops staff-room scheduling otherwise (`api/app/channels/trust_monitor/publisher.py:38-73`). Matrix member and receipt callbacks call synchronous ingest via `asyncio.to_thread` (`api/app/channels/plugins/matrix/trust_monitor_handler.py:57-113`); ingest calls the publisher and records `notified` unconditionally (`api/app/channels/trust_monitor/service.py:187-198`). Wiring sets only the notifier coroutine (`api/app/channels/plugins/matrix/channel.py:410-464`), not its owner loop. | TDD: bind the main loop during Matrix startup, use `create_task` on-loop and `run_coroutine_threadsafe` off-loop, surface scheduling success, and avoid recording `notified` when scheduling fails. Flag actual Matrix delivery for human validation. |
| H10 | **CONFIRMED** | `ContainerDown` selects `up{job="cadvisor", container=...}` at `docker/prometheus/alert_rules.yml:30-39`, but that scrape target is defined only by job/instance (`docker/prometheus/prometheus.yml:31-34`). `ContainerUnhealthy` queries `container_health_status` at `docker/prometheus/alert_rules.yml:41-49`, and no producer exists. The same rules file already uses real cAdvisor `container_last_seen{name=~"docker-.*"}` at `docker/prometheus/alert_rules.yml:246-266`. | Replace both dead expressions with real cAdvisor presence/freshness series and add `promtool` rule tests where practical. |
| H11 | **CONFIRMED** | Every receiver posts to the monitored API at `docker/alertmanager/alertmanager.yml:53-71`. Compose explicitly says the standalone relay was removed and API forwarding is used (`docker/docker-compose.yml:511-513`). API failure therefore removes the notification path for API-down alerts. | Restore an out-of-process alert relay and point Alertmanager receivers to it. **Human/production validation required:** Matrix credentials, room routing, resolved-alert behavior, and an actual API-container-down drill cannot be proven locally. |
| H12 | **CONFIRMED** | Grafana hard-exits when either secret is absent (`docker/docker-compose.yml:367-394`). Only `deploy.sh` creates/injects them (`scripts/deploy.sh:254-267,315-326`). `update.sh` calls shared validation (`scripts/update.sh:29-58`), but `validate_runtime_configuration` checks only trust-monitor and ChatOps dependencies (`scripts/lib/common.sh:274-312`). | Add update-safe, non-rotating provisioning/validation for both Grafana secrets with shell tests. **Human/production validation required:** exercise an upgrade from a legacy `.env`, verify file ownership/permissions, and verify Grafana plus its datasource without exposing values. |
| H13 | **MODIFIED** | The “only feedback” premise is overstated: feedback rejection is tested at `api/tests/routes/test_admin_access_security.py:6-22`, and escalation rejection at `api/tests/routes/test_escalation_endpoints.py:280-291,357-368`. The broader gap remains: eleven protected routers are mounted at `api/app/routes/admin/__init__.py:40-51`, auth is attached per router, and no exhaustive invariant covers every protected `/admin` route. | Add an application-level parametrized auth test over every protected `/admin` route/method, excluding intentional `/admin/auth/*`, and a structural dependency assertion for clearer failures. |
| H14 | **CONFIRMED** | `scripts/tests/test_env_config.sh:1-12` is standalone and CI never invokes it (`.github/workflows/ci.yml:10-147`). Existing Python deployment tests exercise helpers, but top-level `rollback.sh`/`update.sh` orchestration remains untested; both execute orchestration directly (`scripts/rollback.sh:237-274`, `scripts/update.sh:656-698`). | Add a shell CI job with shellcheck and `test_env_config.sh`. Add shimmed subprocess tests for rollback preconditions/argument handling and the authoritative-FAQ migration-skip guard, with no real Docker/Git mutation. |
| H15 | **CONFIRMED** | Public `/feedback/stats` reads `feedback_*.jsonl` at `api/app/routes/feedback_routes.py:106-138`. Runtime writes use SQLite at `api/app/services/feedback_service.py:162-212`, and the repository already exposes aggregate stats at `api/app/db/repository.py:849-904`. | Read SQLite stats off-thread, map `total`/`positive_rate` to the existing public schema, and test that SQLite wins even if a conflicting legacy JSONL file exists. |

## Converged MEDIUM items explicitly mentioned by the task prompt

These do not change the C/H verdicts above.

| ID | Verdict | Evidence and disposition |
|---|---|---|
| M2 | **MODIFIED — defer to backlog** | `scripts/update.sh:557-563` calls a missing migration module, but `.gitignore:88-90` excludes both the authoritative FAQ database and the former JSONL source. The old script and its dependencies were intentionally removed. Restoring only the script cannot populate a fresh clone and risks reviving an automatic overwrite path. A safe bootstrap needs a human-selected canonical snapshot/source with fail-closed integrity checks, so this is not the promised cheap win and will not be implemented in this run. |
| M9 | **CONFIRMED — include with observability** | `docker/prometheus/alert_rules.yml:78-86` uses nonexistent `feedback_satisfaction_rate` and compares it as a 0–1 ratio. The emitted metric is `bisq_feedback_helpful_rate` and is set as a percentage at `api/app/main.py:781-798`. Change the rule to the real metric and a `< 60` threshold. |
| M10 | **CONFIRMED — include with observability** | `scripts/deploy.sh:378-407` compares healthy containers to all services even though some services lack healthchecks, and its timeout branch does not exit. Make the gate fail closed after M11 supplies the missing checks. Flag the full deploy path for human validation. |
| M11 | **CONFIRMED — include with observability** | Grafana (`docker/docker-compose.yml:367-417`), node-exporter (`docker/docker-compose.yml:418-445`), and scheduler (`docker/docker-compose.yml:515` onward) have no healthchecks. Add service-appropriate checks; scheduler needs a process/heartbeat check rather than a superficial container-running check. |

## Implementation decision summary

- Implement all 18 C/H findings using the corrected plans above.
- Treat C1, C3, H3, H6, H8, and H13 as corrected (**MODIFIED**) fixes,
  not the automated report's literal remediations.
- Include M9–M11 because they converge directly with the required observability
  batch.
- Defer M2 and all other MEDIUM/LOW findings to the ordered backlog.
- H11 and H12, plus real Matrix/Bisq delivery behavior for H5–H9, require
  explicit human validation after local tests. No deployment is authorized in
  this task.
