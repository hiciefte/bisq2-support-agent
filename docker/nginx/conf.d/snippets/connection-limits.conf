# Connection limits configuration

# Default connection limit for most endpoints
# Set to 100 to accommodate Tor exit nodes with shared IPs
limit_conn addr 100;

# Return 429 status code when connection limit is reached
limit_conn_status 429; 