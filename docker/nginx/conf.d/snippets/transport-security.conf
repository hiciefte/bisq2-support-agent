# The containing server selects the value. HTTP and Tor-only servers use an
# empty value, which makes nginx suppress the header; the TLS server sets HSTS.
add_header Strict-Transport-Security $strict_transport_security always;
